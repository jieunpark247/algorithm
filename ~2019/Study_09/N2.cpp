#include <string>
#include <vector>

using namespace std;
vector<vector<int>> house;

int solution(int N, vector<vector<int>> house) {
    int answer = 0;
    return answer;
}
int main(){
    int x,y;
    do {
        scanf("%d%d", &x, &y);
        vector<int> a(100);
        house[x][y]=1;
        // value값을 parameter로하는 함수호출부분
    } while (getc(stdin) == ' ');
    
    for(int i)
    
}
